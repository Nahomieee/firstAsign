package Exercise4;

public class Spellchecker {
    public boolean isLetter(char sign) {
        boolean isLetter = false;

        if (sign> 'a' && sign<'f') {
            isLetter = true;
        }

        return isLetter;

    }}
